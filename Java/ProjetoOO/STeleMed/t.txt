Observações:

Como eu poderia implementar a função abaixo na classe Administrador, com as seguintes funcionalidades:

public void realizarLogin() {
        System.out.print("Insira o email: ");
        String email = leitor.nextLine();
        System.out.print("Insira a senha: ");
        String senha = leitor.nextLine();

        Faz a verificação para ver se o Administrador possui um login. (recebendo por parametro o arquivo que contem a lista de Administradores e busca as informações inseridas anteriormente).

            Se sim, chama a funcao visualizarMenu().

            Se nao, pede para ele realizar um cadastro, chamando a funcao cadastrarUsuario() que está na classe ListaAdministradores.

    }